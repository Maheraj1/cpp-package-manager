#include "PackageManagerDelegate.hpp"

namespace PkgM {
	
	void PackageManagerDelegate::Setup(::CLIB::API::AppCommand* appCommand) {
		// appCommand->RegisterSubCommand(Command *subcommand);
	}

	void PackageManagerDelegate::Cleanup() {
		
	}
}